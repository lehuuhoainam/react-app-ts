/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import listUserSaga from '../saga';

// const generator = listUserSaga();

describe('listUserSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
