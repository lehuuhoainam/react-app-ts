/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import sigupSaga from '../saga';

// const generator = sigupSaga();

describe('sigupSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
